# kaggle个性化时尚推荐

数据量为：10万商品，100万用户，3000万交互

任务目标：预测最后一周用户会购买的物品，MAP@12评估

使用6周的数据作为训练，1周数据作为验证



特征工程

+ 删除无用特征：给定的数据中包含同一种属性给了字符串也给了编码，删除字符串对应的列。
+ 缺失值：众数填充
+ 年龄特征：分组
+ 交互数据：时间，uid,tid,price,channel 后两个没用到



召回方法

+ itemcf：召回公式$$w_{ij} = \dfrac{\sum_{u \in U_i \cap U_j} \dfrac{1}{\log(1+|U_u|)}}{\sqrt{\text{item\_cnt}[i] \cdot \text{item\_cnt}[j]}}$$ 对共现用户施加惩罚，以提高长尾商品的推荐效果；用户行为序列做权重——离操作时间越近权重越大dt = (max_time - t) / DAY，w_t = 1 / (1 + dt)
+ 复购策略：根据用户历史购买记录，优先推荐用户购买过的商品，并根据购买距离当前的时间进行分数衰减
+ 流行物品：计算一周内热门的物品，使用物品被购买的时间距离当前时间做时间权重，时间越近权重越大，1/(1+(max_time-pl.col('time'))/DAY)
+ lightgcn：正样本与负样本遵循1:5，使用交叉熵损失，负样本随机采样；得到用户/物品向量后做矩阵乘法，返回分数最大的。


特征工程与排序模型：

+ 样本
  + 召回物品中用户实际购买了的为正例，未购买的为负例，排除未召回但购买的物品作为正例能够提升最终的分数，原因可能是使用这些物品训练模型会带来SSB
  + 由于召回结果中正负例的比例非常不均衡，因此这里只保留了1:15的负例
+ 特征
  + 物品，用户本身属性
  + 统计特征：一段时间内用户购买的商品数，商品被购买的次数，用户复购率，商品被复购率，用户-物品最后一次时间差
  + 相似度：Word2vec item2item——使用gensim将将用户的购买序列看作句子，物品看作词，训练Word2Vec模型得到物品Embedding，使用点击计算相似度；BPR相似度，通过矩阵分解计算得到user/item的Embedding，用内积计算相似度得分。使用implicit库快速实现。
  + 召回属性：itemcf得分，召回来源，召回通道数
+ 模型：lightgbm目标函数用lambdamark（考虑相对顺序，训练样本必须按照query划分），比较适合于需要优化TOPK的场景。200棵树，学习率为0.05



完整的数据集是一百万个用户的1500万历史点击，从中选择5万个用户以及他们的历史点击

新闻的词向量来自于公开的预训练语料库glove，在一个impression内按照1:4抽样(作为candidates)，**负样本不够会重复抽样**

用户的历史点击和新闻标题长度会被截断，使用交叉熵损失

加性注意力池化中会使用mask，因为标题和用户序列中有填充，需要设置为极小的值防止影响softmax输出

自动混合精度上下文，让GPU自动使用更低的精度，减少显存，加速计算





