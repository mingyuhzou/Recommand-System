# kaggle个性化时尚推荐

数据量为：10万商品，100万用户，3000万交互

任务目标：预测最后一周用户会购买的物品，MAP@12评估

使用6周的数据作为训练，1周数据作为验证



召回方法

+ itemcf：引入时间权重，引入时间衰减权重（反比例函数），两个物品购买间隔（$dt$）越短，权重越高；$cij / (\sqrt{item\_cnt[i] * item\_cnt[j]} * \log(item\_cnt[i] + 10))$ 对热门物品进行打压，以提高长尾商品的推荐效果；用户行为序列做权重——里操作时间越近权重越大
+ 复购策略：根据用户历史购买记录，优先推荐用户购买过的商品，并根据购买距离当前的时间进行分数衰减
+ 流行物品：计算一周内热门的物品，使用物品被购买的时间距离当前时间做时间权重，时间越近权重越大，1/(1+(max_time-pl.col('time'))/DAY)
+ lightgcn：

特征工程与排序模型：

+ 样本
  + 用户实际购买了的为正例，未购买的为负例，排除购买未召回的物品作为正例能够提升最终的分数，原因可能是使用这些物品训练模型会带来SSB
  + 由于召回结果中正负例的比例非常不均衡，因此这里只保留了1:30的负例
+ 特征
  + 物品，用户本身属性
  + 统计特征：一段时间内用户购买的商品数，商品被购买的次数，用户复购率，商品被复购率，用户-物品最后一次时间差
  + 相似度：Word2vec item2item——使用gensim将将用户的购买序列看作句子，物品看作词，训练Word2Vec模型得到物品Embedding，使用点击计算相似度；BPR相似度，通过矩阵分解计算得到user/item的Embedding，用内积计算相似度得分。使用implicit库快速实现。
  + 召回属性：itemcf得分，召回来源，召回通道数
+ 模型：使用lightgbm用lambdamark训练



新闻的词向量来自于公开的预训练语料库glove，会阶段用户序列和新闻标题长度，在一个impression内按照1：4抽样，负样本不够会重复抽样
加性注意力池化中会使用mask，因为标题和用户序列中有填充，需要设置为极小的值防止影响softmax输出